function getAge() {
  "use strict";
  //   var args = 21;
  console.log(args);
  console.log(typeof args);
}

getAge();

// in use strict use must define the variable scope
//not hoisting concept is  used in use strict
